# Run records

Per-run execution history for this project, one run record per file, each
validating against `.guild/core/schemas/run-record.schema.json`. Currently
holds two illustrative example runs (`add-feature`, `fix-bug`) generated
during Guild's own bootstrap.

See `.guild/core/templates/run-record.template.yaml` for the shape of a new
run record.
