# Run records

Not yet populated. This directory holds per-run execution history once
workflows are executed (Phase 3 onward), one run record per file, each
validating against `.guild/schemas/run-record.schema.json`.

See `.guild/templates/run-record.template.yaml` for the shape of a new run
record.
